# MVP decision log

prompt | decision | reason
---|---|---
P0b | Package paths `src/drishtisr/<pkg>` read as `src/<pkg>`; predictor written to `src/infer/predictor.py` | The repo has no `src/drishtisr/`; `drishtisr` is a root alias package mapping to `src/` (drishtisr/__init__.py).
P0b | Python mechanism = venv interpreter + cwd/PYTHONPATH = worktree root; no `scripts/mvp/_py.py` | `PYTHONPATH=<worktree>/src` cannot import `drishtisr` (alias lives at root); no editable install exists, so the root path already resolves inside the worktree.
P0b | TorchPredictor runs `tiled.sr_array`; inputs ≤ tile use one tile of their own size | Eval never calls tiled.py (it calls `model(lr)`); a single unpadded tile makes the predictor bit-identical to eval (measured max diff 0.0), larger inputs tile with cfg.frontend.tile (256/32).
P0b | `info.model_bytes` = fp32 weight bytes (3,422,608 for A2), not the .pt file size | The .pt also stores optimiser/scaler state (10.3 MB); weight bytes are comparable to an ONNX file size.
P0b | Scale-head models: run sr_array twice (mean, then scale); b = sqrt(exp(logvar)/2) | tiled.py sizes its output from the input channels and cannot carry 8 channels; editing it is forbidden. Conversion matches a Laplace to the Gaussian head's variance; P6 may register its own predictor.
P0b | Smoke VAL patch = first cached VAL id in split-CSV order, 64-px centre crop, read from the npz cache directly | The dataset catalog load needs the network (tacoreader); cfg.patches.lr_size is 64.
P0b | Main-tree root found in tests via `git rev-parse --git-common-dir`; tests SKIP (with path) when artefacts are absent | AGENTS.md forbids hardcoded paths and requires tests to run without data.
P0b | Fixture metrics: with_gt from Day 3 CSV split means (A2, bicubic); other numbers placeholders; a FIXTURE warning string is included | The contract has no fixture flag; the warning keeps a UI screenshot from being mistaken for a measurement.
P0b | Fixture refs unc_display_max = cons_display_max = 0.02 reflectance | Placeholder above the 0.0057 consistency floor; P10/P5 own the real values.
P10 | UI look: dark neutral viewer, one accent #2a78d6; amber #fcd34d only for AI label / INTERIM chip / blur guard; emerald-600 API-online badge | inventory §14: no stand-alone mock UI; colours reused from frontend/ and the report palette.
P10 | Legend ramps fixed: uncertainty = inferno stops, consistency = viridis stops (app/static/styles.css, same stops in scripts/mvp/make_ui_fixtures.py); ticks 0 / max/2 / ≥max | Contract fixes the scale but not the colormap; the overlay PNG producer (P4/P5) should use these stops or the legend misreads.
P10 | Fixture PNGs are 512×512 although sample_response sr_size is 256; thumb_val/thumb_delhi.png (96×96) generated but unreferenced | Prompt specifies 512 and 96 px; samples.json (not owned) points thumbs at lr_rgb.png; the viewer fits images, so size does not matter.
P10 | test_ui_static checks /fixtures/ URLs exist and skips /files/ download URLs | /files/ are server job outputs; mock mode shows the download links disabled.
P10 | Mock mode derives the no-GT (non-has_gt sample or upload) and TTA-off variants client-side from the single fixture and appends a "MOCK MODE" warning | Exercises both contract metric layouts offline without inventing fields.
P10 | Model size shown as model_bytes/1000 "KB"; no-GT consistency card uses refs floors as an "HR floor (VAL)" marker | Contract gives only bytes; spec_l1_hr is absent when with_gt is null.
P4 | ONNX graph exported with dynamic batch, H and W (not static 256×256) | tiled.py pads edge tiles to the full `tile`, but TorchPredictor (mirrored by OnnxPredictor) passes tile = max(h, w) for inputs ≤ one tile, so 64-px patches and 130-px VAL tiles arrive at their own size.
P4 | OnnxPredictor reuses TorchPredictor._tiling and __call__ as the same functions; package-internal imports are relative | One validation/tiling/tiled.py code path; relative imports keep `src.*` and `drishtisr.*` imports from splitting the predictor registry.
P4 | Calibration/parity TRAIN tiles = random 64-px LR crops (export trace size = cfg.patches.lr_size) of distinct cached TRAIN ids, seed 26142; gate = first 64 cached VAL ids, centre 64-px crops, raw session (≡ tiled single tile) | Prompt says "export tile shape"; with dynamic axes the trace shape is 64. Calibration runs in 8-tile slices via ORT CalibStridedMinMax (CalibMaxIntermediateOutputs is broken for MinMax in ORT 1.20; Percentile would otherwise hold ~11 GB of activations with 2.3 GB free).
P4 | Benchmark input = 256×256 window (rows/cols 400) of M\data\delhi\delhi_A_20241030.tif, (dn−1000)/10000 inside the timed region; peak RSS null (psutil not installed) | Real Sentinel-2 input at the requested size; the project has no normalisation beyond DN→reflectance.
P4 | No Co-Authored-By trailer on the P4 commit | RULES.md Git section forbids it; it overrides the harness default.
P5 | VAL patch pool = all 300 VAL tiles (split-CSV order) × 2×2 grid of 64-px LR patches in the 128-px centre crop (cfg.sr.lr_patch_size, cfg.patches stride 64); draws = default_rng(26142).choice without replacement; npz cache read directly | Mirrors the val grid (≈4 patches/tile) without the network-bound catalog; one pool shared by calibration and projection eval.
P5 | TTA identity member reuses the single-pass SR; the other transforms are batched per transformed shape | Same deterministic computation; saves one forward pass. timings_ms.uncertainty covers only the extra passes + std.
P5 | spec_l1 / spec_sam_deg scalars from src.losses.spectral.spectral_terms; per-pixel cons_sam_deg map uses its numerics (eps inside norm, cos clamp), 0 where a spectrum norm < DEFAULT_MIN_NORM | No second operator; map needs per-pixel values that spectral_terms only reduces.
P5 | HF ratio = src.metrics.sharpness.hf_energy_ratio (cutoff 0.25); FFT fallback not needed | Found in inventory §7.
P5 | Sobel baseline map = per-pixel band-mean Sobel magnitude of bicubic using sharpness.SOBEL_X/Y + replicate pad | gradient_magnitude only returns the image mean; kernels imported, not redefined.
P5 | learned_laplace: timings_ms.uncertainty = 0.0; projection time (project_iters>0) folded into timings_ms.sr, no new key | Contract runtime_ms has exactly sr/uncertainty/total.
P5 | project_consistency + compute_trust(project_iters=0) shipped in the step-10 commit; eval/test/gate committed separately after | Default 0 keeps behaviour identical; splitting the function out would only churn trust.py.
P5 | Projection gate HF = mean over patches of hf_energy_ratio(x)/hf_energy_ratio(HR); gate on 200-patch VAL means | Same per-patch definition as Day 3 hf_ratio_vs_gt; the gate is relative between variants.
P5 | TTA timing on the 128-px centre crop of a VAL tile, median of 3 after warm-up; CPU load measured just before (13% → provisional=false) | RULES Evidence section.
P5 | No Co-Authored-By trailer on P5 commits | RULES.md Git section forbids it.
P5 | Projection gate FAILED at iters 1/2/3 (LPIPS ×1.057–1.069 > 1.01, HF ×0.90–0.91 < 0.95; spec_l1 ×0.16–0.02 passes); not served. opensr measure skipped (projected 740 s > 600 s budget) | Pre-registered gate, not tuned; RULES: projection served only if passed=true.
P4 | INT8 failed all four ladder rungs for a2-last-dce224ec; default_backend = onnx-fp32; bench times the closest-to-gate attempt (C) as onnx-int8 with gate_passed=false | Prompt requires a latency for three backends; the failing INT8 graph is never served (OnnxPredictor precision=auto reads quant_gate.json).
P6 | src/uncertainty/ package shadows legacy src/uncertainty.py; __init__ loads it by path and re-exports tta_predict/sr_output/TTAResult | Prompt mandates the package path; tests/test_tta.py and scripts/calibrate_uncertainty.py import src.uncertainty and still pass.
P6 | 8-channel packed output served by running tiled.sr_array once per 4-channel half (ChannelSlice), like P4's OnnxPredictor | sr_array sizes its output buffer from the input band count; tiled.py must not be edited. b is still Hann-blended linearly.
P6 | TRAIN stream = SRPatchDataset wiring rebuilt in train_unc.py with loader.split_file pinned to M\outputs\splits_sen2naipv2.csv (asserted file: source); cached_only drops uncached TRAIN tiles | SRPatchDataset has no override hook and recomputes the split when the worktree has no CSV.
P6 | frac_floor computed over per-band b values (not band mean) | Stricter reading of "share of pixels with b < 2e-4"; more likely to flag collapse.
P6 | Eval patches = P5 VAL pool, seed 26143, excluding the 128-patch watch subset (seed 26142) | Watch data is monitoring-only; keeping it out of scoring is the conservative choice.
P6 | b0 = mean |SR-HR| over 128 TRAIN patches, drawn by default_rng(26142) from the epoch-0 augmented TRAIN stream | Prompt: TRAIN, 128 patches; seed reuses the MVP seed.
P6 | No Co-Authored-By trailer on P6 commits | RULES.md Git section forbids it.
P7 | Selection source = eval_all_ckpts full-VAL per-pair caches (M\outputs\metrics\eval_all_ckpts\<run>\itNNNNN), rule = src.eval.ckpt_selection.select_checkpoint with cfg.eval_all_ckpts.selection, seed cfg.seed | Prompt: wrap the pre-registered rule (inventory §12); the caches hold opensr `reflectance` + LPIPS for every saved checkpoint, so no re-evaluation. sel-v1 thresholds not implemented (RULES Day 3 addendum).
P7 | Label → file via checkpoint payload `it`+1; best.pt coincides only for Run A (it8000); A2/B1/B2 choose last.pt (it12000), matching day3_ab_summary.json | No it*.pt files exist; iteration is the only join key.
P7 | Run A row = eval_all_ckpts/runA/it08000 per-pair means (same evaluator, 1,199 VAL); HF/Sobel shown "not measured"; cfg hash / SHA / dirty "—" | Day 3 sharpness cache covers A2/B1/B2 only; Run A predates run_metadata.json. Not re-evaluated (would be a second sharpness pass).
P7 | Projection row = proj1 (smallest iters) from projection_gate.json, n=200 subset, gate FAILED; opensr cell "not run"; Δ vs A2 on the gate's own 200-patch A2 | Gate has iters=null; proj1 is what would be served first. No % vs bicubic (different patch set).
P7 | Deployment Δ = backend − FP32 on the 64-patch quant-gate VAL set; INT8 row = the attempt whose bytes match the benched file (C); headline INT8 carries gate_passed=false, served=false | bench note names attempt C; sign stated explicitly to avoid the quant_gate fp32−int8 convention.
P7 | Headline HR reference = day3_results.json floor.reflectance (0.004782, HR in place of SR, opensr measure) | Same measure as the winner/bicubic numbers.
P7 | --split test reads reports/mvp/test_ab_metrics.csv (Day 3 CSV columns; pending if absent) and writes *_test outputs | No test-split evidence exists and test must not overwrite VAL tables; P7 does not run a test evaluation itself.
P7 | Uncertainty table: schema-tolerant reader for unc_eval_*.json (methods/method/top-level layouts, key aliases); all rows pending now | P6 has not written its reports; schema not yet fixed.
P7 | No Co-Authored-By trailer on the P7 commit | RULES.md Git section forbids it.
P9 | Backend auto → onnx-fp32: quant_gate.json passed=false for a2-last-dce224ec; explicit onnx-int8 also falls back to torch-fp32 with a startup warning (no model_int8.onnx exists) | Failing INT8 graphs are never served; missing ONNX → torch-fp32 + warning in every response.
P9 | Projection off (project_iters=0): projection_gate.json passed=false | Prompt rule; DRISHTI_PROJECT=0 also disables it if a later gate passes.
P9 | with_gt metrics via src.metrics.aggregate.Evaluator.metrics_for_batch (psnr_mean, ssim_mean, sam_mean_deg, ergas, lpips) on CPU | Same call eval uses, so LPIPS band handling (RGB by name, clip on a copy) is identical.
P9 | runtime_ms = compute_trust timings (sr, uncertainty, total); rendering/GeoTIFF/GT-metric time excluded | Matches P5's timing definition; end-to-end request time is reported separately in reports/mvp/api_latency.json.
P9 | Uncertainty overlay uses render.py's magma LUT (UI legend = inferno stops); consistency = viridis | render.py has only magma/viridis LUTs; magma ≈ inferno, and editing render.py is outside P9's owned files.
P9 | Samples: 6 TEST (seed 26142, 200-id cached-TEST subset, 128-px centre crops, HR HF-energy terciles, ranks 1/3 and 2/3 of each) + 2 Delhi 256×256 crops (A centre = dense urban; B stride-128 window with NDVI>0.3 fraction closest to 0.5, p99 ≤ 1 = mixed/agricultural), (dn−1000)/10000 | Deterministic; LR ≤ 512; Day 2 formula from tiled.run_file --dn-offset 1000.
P9 | Delhi labels = "Delhi · Sentinel-2 L2A · no ground truth (dense urban)" / "(mixed / agricultural)" | The two crops would otherwise have identical labels in the sample picker.
P9 | Samples total 30.7 MB ≥ 25 MB → not committed (app/samples/.gitignore); rebuild with `python scripts/mvp/build_samples.py` (venv python, cwd/PYTHONPATH = worktree root, needs main-tree cache + data/delhi) | Prompt size rule; root .gitignore already ignores *.npy/*.tif.
P9 | Sample thumbnails served at /api/samples/{id}/thumb.png (only thumb.png, id regex-checked) | Contract gives thumb_url as a string without a route; no general static mount over app/samples.
P9 | Same-origin: no CORS middleware; a POST whose Origin host ≠ Host gets 400 {"error"}; invalid tta/dn_mode → 422, XOR/size/bands/unknown sample → 400 | Contract lists 400/422/500 only.
P9 | Non-finite scalar metrics (e.g. constant image, bicubic HF = 0) are returned as null with a warning | Starlette refuses NaN in JSON; this only happens on degenerate inputs.
P11 | E2E (tests/mvp/test_e2e.py, real server subprocess) found no contract mismatch between the live API and app/fixtures/*.json; no fixes made in app/** or src/infer/** for it | 7/7 passed on the first run.
P11 | DRISHTI_UNC_CKPT (opt-in, unset by default) wired in app/pipeline.py Engine._load_predictor via P6's src.uncertainty.head.ScalePredictor (EDSRWithScale.forward_packed through tiled.sr_array); served backend torch-fp32; other DRISHTI_BACKEND values ignored with a startup warning; checkpoint_id = head id | Prompt step 4 (watch verdict "passed"). No contract change: trust.compute_trust already maps has_scale_head → learned_laplace and the response validates against the P9 schema.
P11 | Learned-head overlay reuses refs.unc_display_max (0.00371, TTA-8 std scale): on test-high-1 unc_mean 0.0092 / p95 0.0177 → the overlay saturates. Not fixed; P12 item | Contract has one unc_display_max; recalibrating trust_scales.json is outside P11's owned files.
P11 | E2E server searches ports from 8100 upward, not 8000 | A running demo on 8000 is not disturbed; serve.py itself starts at 8000.
P11 | serve.py builds Engine before uvicorn (create_app(engine)) to print backend/checkpoint_id/interim; OMP/MKL threads default to DRISHTI_THREADS (6) unless already set | One engine load; env override kept.
P11 | HF Space image also stages the A2 .pt at ckpt/a2/last.pt (DRISHTI_CKPT), plus configs/, drishtisr/ and reports/mvp/projection_gate.json | Engine and OnnxPredictor derive checkpoint_id from the .pt sha256 and parent dir name; load_config reads configs/base.yaml; imports go through the drishtisr alias. No app change.
P11 | Space image pre-downloads the LPIPS AlexNet weights at build time and runs as uid 1000 with writable app/_jobs | with_gt samples compute LPIPS; the first request would otherwise download ~233 MB. HF runs containers as uid 1000.
P11 | make_tables --split val rerun changed only uncertainty_table.md (3 rows filled), ablation.json and headline.json provenance (git_sha, timestamp) | tta4/tta8/learned unc_eval JSONs already existed, so the uncertainty evals were not rerun.
P11 | Space Dockerfile not build-tested: staging (robocopy, 47.9 MB, scratchpad) succeeded; `docker build` failed because the Docker Desktop daemon was not running; not started | Starting Docker Desktop is outside the session's scope; logged as a P12 pre-publish check.
P11 | No Co-Authored-By trailer on the P11 commit | RULES.md Git section forbids it.
